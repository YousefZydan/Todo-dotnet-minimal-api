apiVersion: v1
kind: Service
metadata:
  name: todo-dotnet-api
  namespace: default
spec:
  selector:
    app: todo-dotnet-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8081
  type: ClusterIP

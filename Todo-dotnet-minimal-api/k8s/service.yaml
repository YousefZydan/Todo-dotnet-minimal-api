apiVersion: v1
kind: Service
metadata:
  name: todo-dotnet-api
  namespace: default
spec:
  selector:
    app: todo-dotnet-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP